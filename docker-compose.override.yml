services:
  postgres:
    ports: !override
      - "5433:5432"
  backend:
    ports: !override
      - "8081:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/pcori?stringtype=unspecified
