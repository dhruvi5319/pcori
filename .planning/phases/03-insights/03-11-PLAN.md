---
phase: 03-insights
plan: "11"
type: execute
wave: 4
depends_on: ["03-07", "03-08", "03-09", "03-10"]
files_modified:
  - frontend/src/app/(protected)/layout.tsx
  - frontend/src/components/layout/Sidebar.tsx
autonomous: false

features:
  implements: ["FR-4.1", "FR-4.2", "FR-4.3", "FR-4.4", "FR-4.5", "FR-4.6", "FR-5.1", "FR-5.2", "FR-5.3", "FR-5.4", "FR-5.5", "FR-8.1", "FR-8.2"]
  depends_on: []
  enables: []

must_haves:
  truths:
    - "Sidebar navigation shows Analytics and Data Pipeline links for MANAGER and ADMIN roles"
    - "All 4 Phase 3 pages (/dashboard, /analytics, /data-pipeline) are reachable from the sidebar"
    - "Clicking Analytics in sidebar navigates to /analytics; Data Pipeline navigates to /data-pipeline"
    - "Active nav item is highlighted when user is on the corresponding page"
    - "All Phase 3 Playwright tests pass end-to-end with backend running"
  artifacts:
    - path: "frontend/src/components/layout/Sidebar.tsx"
      provides: "Updated sidebar nav with Analytics (BarChart3 icon) and Data Pipeline (Workflow icon) links"
  key_links:
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "/analytics route"
      via: "Next.js Link component with href=/analytics"
      pattern: "href.*analytics"
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "/data-pipeline route"
      via: "Next.js Link component with href=/data-pipeline"
      pattern: "href.*data-pipeline"
---

<objective>
Add the Phase 3 nav items (Analytics, Data Pipeline) to the existing sidebar. These routes are already built (Plans 07–10) but not yet linked in the sidebar navigation. Also serves as the integration wave — all plans must be complete before this plan runs.

Purpose: Completes the navigation layer so all Phase 3 features are reachable. Final integration checkpoint to ensure full Phase 3 feature set is wired end-to-end.
Output: Updated Sidebar.tsx with Phase 3 nav items.
</objective>

<feature_dependencies>
Implements: FR-4.1–FR-4.6: Dashboard/Analytics nav, FR-5.1–FR-5.5: Data Pipeline nav, FR-8.1–FR-8.2: Notifications already in header (Plan 10)
Depends on: All Wave 3 plans (03-07, 03-08, 03-09, 03-10) — routes must exist before sidebar links are added
Enables: None (Phase 3 complete)
</feature_dependencies>

<execution_context>
@/root/.config/opencode/pivota_spec-framework/workflows/execute-plan.md
@/root/.config/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@frontend/src/components/layout/Sidebar.tsx
@.planning/phases/03-insights/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Analytics and Data Pipeline nav items to sidebar</name>
  <files>
    frontend/src/components/layout/Sidebar.tsx
  </files>
  <action>
Read the existing Sidebar.tsx first. The sidebar was built in Phase 1 with role-gated navigation items. The existing items are:
- Dashboard (all roles)
- Classifications (all roles)
- Taxonomy (ADMIN/MANAGER)
- (possibly others)

Add the following nav items in the correct order. Both items should be visible only to MANAGER and ADMIN roles (same gating as Taxonomy):

```
Analytics        — icon: BarChart3 (lucide)  — href: /analytics      — roles: MANAGER, ADMIN
Data Pipeline    — icon: Workflow (lucide)    — href: /data-pipeline  — roles: ADMIN only
```

Suggested sidebar order after this change:
1. Dashboard (all roles)
2. Classifications (all roles)
3. Analytics (MANAGER, ADMIN)
4. Data Pipeline (ADMIN)
5. Taxonomy (MANAGER, ADMIN)

Role-gating pattern: Match EXACTLY how existing role-gated items are done in the current Sidebar.tsx. If using `isAdmin` or `user?.roles?.includes('ADMIN')` pattern, follow the same approach.

For `BarChart3` and `Workflow` icons — both are in lucide-react (already installed). Import them alongside existing lucide imports.

Do NOT change sidebar styles, glassmorphism, collapse behavior, or any other nav items. Only ADD the two new links in the correct position.
  </action>
  <verify>
```bash
cd frontend && npm run build 2>&1 | tail -10 && echo "BUILD OK"
grep -c "BarChart3\|/analytics" frontend/src/components/layout/Sidebar.tsx
grep -c "Workflow\|/data-pipeline" frontend/src/components/layout/Sidebar.tsx
```
  </verify>
  <done>
- Sidebar.tsx has Analytics nav item with BarChart3 icon → /analytics
- Sidebar.tsx has Data Pipeline nav item with Workflow icon → /data-pipeline
- Role gating matches existing sidebar patterns
- npm run build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 3 end-to-end integration verification</name>
  <what-built>
Complete Phase 3 feature set:
- Dashboard with live KPI cards, sparklines, drag-to-reorder, status breakdown, quick actions, recent classifications feed
- Analytics page with 6 chart sections (Recharts), shared date context, loading pulse
- Data Pipeline page with stage cards, control buttons (admin), logs panel, run history
- In-app notification bell + slide-out drawer + preferences modal
- Sidebar navigation updated with Analytics and Data Pipeline links
- All Playwright e2e tests across 4 spec files
  </what-built>
  <how-to-verify>
1. Start the full stack: `docker compose up -d && cd backend && ./mvnw spring-boot:run &` then `cd frontend && npm run dev`
2. Navigate to http://localhost:3000 and log in as admin
3. **Dashboard**: Verify KPI cards load with numbers and sparklines; click "Customize", drag a card to reorder, click "Done" — reload page and confirm order persisted
4. **Analytics**: Click "Analytics" in sidebar → page loads with 6 chart sections; change date range and confirm all charts update simultaneously with loading pulse
5. **Data Pipeline**: Click "Data Pipeline" in sidebar → page shows pipeline status, 3 stage cards (EXTRACT/CLASSIFY/PERSIST with left-border colors), DB health panel, logs/run history tabs
6. **Notifications**: Upload a new PDF plan → wait ~30 seconds → red badge should appear on notification bell; click bell → drawer slides in from right with classification notification
7. **Preferences**: In notification drawer → find preferences → toggle a notification type off, save, confirm "Preferences saved" toast appears
8. **Sidebar**: Confirm Analytics and Data Pipeline links visible; Dashboard/Classifications still present
  </how-to-verify>
  <resume-signal>Type "approved" when all 8 verification steps pass, or describe any issues found</resume-signal>
  <action>Human verification — no automated action required. Follow the how-to-verify steps above.</action>
  <verify>Human completes all 8 verification steps and confirms expected behavior.</verify>
  <done>All 8 verification steps confirmed: KPI cards live, analytics date cascade working, pipeline stage cards visible, notification bell fires on classification, preferences save correctly, sidebar nav links present.</done>
  <files>none</files>
</task>

</tasks>

<verification>
```bash
cd frontend && npm run build 2>&1 | tail -5
# Run all Phase 3 Playwright specs
npx playwright test e2e/dashboard.spec.ts e2e/analytics.spec.ts e2e/data-pipeline.spec.ts e2e/notifications.spec.ts --reporter=list 2>&1 | tail -40
```
</verification>

<success_criteria>
- Sidebar has Analytics (BarChart3 icon, MANAGER+ADMIN) and Data Pipeline (Workflow icon, ADMIN only) nav items
- All 4 Phase 3 routes reachable from sidebar
- npm run build passes
- All Playwright tests across all 4 Phase 3 spec files pass
- Human verification approves end-to-end feature integration
</success_criteria>

<output>
After completion, create `.planning/phases/03-insights/03-11-SUMMARY.md`
</output>
